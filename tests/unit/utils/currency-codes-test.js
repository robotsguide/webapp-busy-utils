import currencyCodesJs from 'dummy/utils/currency-codes.js';
import { module, test } from 'qunit';

module('Unit | Utility | currency codes.js');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.ok(currencyCodesJs);
});
