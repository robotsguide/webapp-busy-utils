import { module, test } from 'qunit';
import {
	elapsedTime,
	elapsedTimeMap
} from 'busy-utils/time';

module('Unit | Time | { elapsedTime, elapsedTimeMap }');

// Replace this with your real tests.
test('it works', function(assert) {
  assert.ok(elapsedTime);
  assert.ok(elapsedTimeMap);
});
